[project]
name = "gi-talker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "discord-py>=2.6.4",
    "python-dotenv>=1.1.1",
]

[tool.uv]
package = true

[tool.uv.sources]
"gi-talker" = "src"
